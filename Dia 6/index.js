// incremento e decremento
// ++, --, +=, -=, *=, /=
// laço de repetição for

let tabuada = Number(prompt("Insira a tabuada desejada"))

for (let i = 0; i <= 10; i++) {
    let result = tabuada * i
    console.log(tabuada + " x " + i + " = " + result)
}