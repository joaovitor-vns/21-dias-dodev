/*
* Funções de arrays
* array.indexOf(elemento do array) -> retorna o índice que esse elemento está
* array.push(elemento do array) -> vai adicionar um elemento, na ultima posição do array
* array.includes(elemento do array) -> retorna se o elemento do array, está nesse array
* array.splice(posição, tamanho) -> vai descatar o elemento na posição desejada, reordenando o array
* array function javascripts -> Mozilla
* array.forEach(numero(representa cada elemento do array) => {console.log(numero)})
* array.filter(x(representa cada elemento do array) => x > 2) -> retorna um array novo atendendo as condições indicadas
* array.sort() -> reordena o array de forma crescente por padrão
*/

